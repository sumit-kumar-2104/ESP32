# ESPectre - ESP32-S2 Configuration
# WiFi CSI-based motion detection with Home Assistant integration
#
# ⚠️ EXPERIMENTAL: This platform has NOT been tested yet.
# Please report your results on GitHub Discussions!
#
# ⚠️ NOTE: ESP32-S2 does NOT have Bluetooth, so only USB and WiFi provisioning are available.
#
# Compatible boards:
# - ESP32-S2-DevKitM
# - ESP32-S2-Saola
# - LOLIN S2 Mini
# - Any ESP32-S2 board
#
# Provisioning:
# - Via USB: Use ESPHome web tools (https://web.esphome.io)
# - Via WiFi: Connect to "ESPectre Fallback" AP and configure

substitutions:
  git_ref: main # branch, tag, or commit
  detection_algorithm: mvs # mvs (default) or ml

esphome:
  name: espectre
  friendly_name: ESPectre
  min_version: 2026.5.0
  # Uncomment the next line if you have multiple ESPectre devices (adds MAC suffix to name)
  # name_add_mac_suffix: true
  project:
    name: francescopace.espectre
    version: ${git_ref}

esp32:
  variant: ESP32S2
  cpu_frequency: 240MHz
  framework:
    type: esp-idf

# Load ESPectre component from GitHub
external_components:
  - source:
      type: git
      url: https://github.com/francescopace/espectre
      ref: ${git_ref}
    components: [ espectre ]
    refresh: always

# ESPectre - WiFi CSI Motion Detection
espectre:
  id: espectre_csi
  detection_algorithm: ${detection_algorithm}

# Dashboard import for easy adoption
dashboard_import:
  package_import_url: github://francescopace/espectre/examples/espectre-s2.yaml@${git_ref}
  import_full_config: true

# WiFi with fallback AP for provisioning
wifi:
  ap:
    ssid: "ESPectre Fallback"

captive_portal:

# NOTE: ESP32-S2 does NOT have Bluetooth - no esp32_improv available

# Improv provisioning via USB
improv_serial:

# Logging
logger:
  #hardware_uart: UART0  # Uncomment for boards with external USB-UART bridges (CH340, CP2102, etc.)

# Home Assistant API
api:

# OTA updates
ota:
  - platform: esphome
