# Codecov Configuration
# https://docs.codecov.com/docs/codecov-yaml

codecov:
  notify:
    wait_for_ci: false

coverage:
  status:
    project:
      default:
        target: 80%      # Overall coverage target
        threshold: 2%    # Allow 2% drop without failing
    patch:
      default:
        target: 70%      # New code coverage target
        threshold: 5%

# Ignore test files and mocks (only track components/espectre/)
ignore:
  - "test/**"
  - "**/mock_*.h"
  - "**/mock_*.cpp"

# Comment on PRs
comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: true  # Only comment if coverage changed

